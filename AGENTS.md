1. 除非我明确要求修改 ESLint 配置，否则不要改动 `eslint.config.js`。
2. 开发页面和功能时，按职责适度拆分代码：将可复用的界面拆成组件，将独立的状态或副作用逻辑提取为 Hook，将与界面无关的纯逻辑提取为工具函数。保持模块职责清晰、依赖关系简单、命名易懂；不要为拆分而拆分，简单且只在一处使用的逻辑可以保留在原处。
3. 一个功能的专用组件、Hook、工具函数和样式应就近放在该功能目录内；只有确实被多个功能复用时，才提取到公共目录。页面目录中的非路由代码放入 `components`、`hooks`、`utils` 等路由生成器忽略的子目录。

## 学习型 Demo

1. 当我要求实现概念、API 或功能 demo 时，以帮助理解为目标：示例要能运行、能观察结果，并说明为什么会得到这个结果；不要只展示一段代码或静态截图。
2. 按请求范围实现。涉及前后端交互时，提供真实可调用的后端接口和前端操作页面；仅涉及单端时，不为凑齐两端而增加代码。
3. 展示页必须为每个例子提供足够独立理解的讲解：先说明它解决什么问题、触发条件和相关概念，再逐步解释请求或操作如何经过关键代码、框架机制与处理器，最后说明预期结果为何出现。标出容易混淆的相近做法及适用边界。讲解贴近对应例子，不能只给一句概述、一段代码和一个结论，也不要堆砌与例子无关的文档。
4. 让关键行为可以实际触发，并展示有助于学习的结果，例如输入与输出、状态码、响应头、错误情况或界面状态。无法直接在页面内呈现的行为，提供合适的操作入口。
5. 优先选择小而真实、彼此可对比的例子；明确演示中为简化而作的选择，不把过时或仅供教学的做法默认为生产推荐。
6. 沿用仓库现有结构和技术栈，适度拆分代码。验证构建、静态检查及关键交互；最终说明运行方式和未能验证的部分。
7. 实际结果旁应结合该例解释关键字段和现象（包括失败、无响应或连接中断），指出它们分别对应哪一步代码与机制；即使用户还没操作，也应能读懂预期会发生什么，操作后能对照预期与实际。验证时逐个检查例子的讲解是否准确且可理解。
